# tree-demo

Sample project used to exercise betterreview's files panel. The point is the
shape of the tree, not the code: four nesting levels, several sibling folders,
and files at every depth.

Open the pull request with `betterreview` and look at panel `[2]`.
